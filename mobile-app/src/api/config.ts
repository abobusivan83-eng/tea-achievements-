/** Таймаут запроса (загрузка медиа может быть долгой). */
export const API_TIMEOUT_MS = 90_000;

/** Повторы только при сетевых сбоях и 5xx (не при 401/403/404). */
export const API_MAX_RETRIES = 2;
