// Production entrypoint.
// - For development use: `npm run dev`
// - For production use: `npm run build` then `node server.js` (грузит dist/src/server.js)
import "dotenv/config";
import "./dist/src/server.js";

